import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import { Link } from 'react-router-dom';
export default function Nav(){const [open,setOpen]=useState(false);return <header className="nav"><div className="nav-brand"><a href="#top" aria-label="Ashes home"><img src="/ashes-logo-transparent.webp" alt="ASHES"/></a><span>AVAILABLE FOR PROJECTS</span></div><nav className={open?'open':''}>{[['Work','#work'],['Expertise','#capabilities'],['About','#studio'],['Process','#process']].map(([a,b])=><a key={a} href={b} onClick={()=>setOpen(false)}>{a}</a>)}</nav><Link className="nav-cta" to="/login">Sign in <span>+</span></Link><button className="menu" onClick={()=>setOpen(v=>!v)} aria-label="Toggle menu" aria-expanded={open}>{open?<X/>:<Menu/>}</button></header>}
