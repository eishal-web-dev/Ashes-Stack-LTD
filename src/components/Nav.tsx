import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import Brand from './Brand';
export default function Nav(){const [open,setOpen]=useState(false);return <header className="nav"><Brand/><nav className={open?'open':''}>{[['Work','#work'],['Expertise','#capabilities'],['About','#studio']].map(([a,b])=><a key={a} href={b} onClick={()=>setOpen(false)}>{a}</a>)}<a className="nav-cta" href="mailto:hello@ashes.studio">Start a project <span>+</span></a></nav><button className="menu" onClick={()=>setOpen(v=>!v)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></header>}