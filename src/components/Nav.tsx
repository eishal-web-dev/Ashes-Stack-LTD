import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import Brand from './Brand';
export default function Nav(){const [open,setOpen]=useState(false);return <header className="nav"><div className="nav-brand"><Brand/><span>AVAILABLE FOR PROJECTS</span></div><nav className={open?'open':''}>{[['Work','#work'],['Expertise','#capabilities'],['About','#studio'],['Process','#process']].map(([a,b])=><a key={a} href={b} onClick={()=>setOpen(false)}>{a}</a>)}</nav><a className="nav-cta" href="mailto:hello@ashes.studio">Start a project <span>+</span></a><button className="menu" onClick={()=>setOpen(v=>!v)} aria-label="Toggle menu" aria-expanded={open}>{open?<X/>:<Menu/>}</button></header>}
