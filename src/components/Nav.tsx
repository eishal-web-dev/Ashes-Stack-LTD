import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import { Link } from 'react-router-dom';
export default function Nav(){const [open,setOpen]=useState(false);return <header className="nav"><div className="nav-brand"><Link to="/" aria-label="Ashes home"><img src="/ashes-logo-transparent.webp" alt="ASHES"/></Link><span>AVAILABLE FOR PROJECTS</span></div><nav className={open?'open':''}>{[['Work','/work'],['Expertise','/expertise'],['About','/about'],['Process','/process'],['Contact','/contact']].map(([a,b])=><Link key={a} to={b} onClick={()=>setOpen(false)}>{a}</Link>)}</nav><Link className="nav-cta" to="/login">Sign in <span>+</span></Link><button className="menu" onClick={()=>setOpen(v=>!v)} aria-label="Toggle menu" aria-expanded={open}>{open?<X/>:<Menu/>}</button></header>}
